{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "uninstall",
            "command": ["pip uninstall flask_plugin -y"],
            "detail": "Uninstall Flask-Plugin previous installation"
        },
        {
            "type": "shell",
            "label": "document",
            "command": ["cd docs && ./build.sh"],
            "detail": "Build document for Flask-Plugin"
        },
        {
            "type": "shell",
            "label": "coverage",
            "command": ["coverage run --source src -m tests.run && coverage html -d reports"],
            "detail": "Check code coverage for Flask-Plugin"
        }
    ]
}